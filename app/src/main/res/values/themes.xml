<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MicroStream" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/ms_theme</item>
        <item name="colorOnPrimary">@color/ms_theme</item>
        <item name="android:colorAccent">@color/ms_theme</item>

    </style>
</resources>